blockquote {
    font-size: 0.4rem;
    background-color: var(--background-footer-color);
    color: var(--link-active);
}

code {
    font-size: 0.6rem;
    background-color: var(--background-footer-color);
}

p {
    font-size: 0.8rem;
    line-height: 1.6rem;
}
